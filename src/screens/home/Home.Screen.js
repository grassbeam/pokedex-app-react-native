import React from 'react';
import {SafeAreaView, Text, TouchableHighlight} from 'react-native';

const HomeScreen = ({navigation}) => (
  <SafeAreaView>
    <Text>WELCOME HOME Pokedex</Text>
  </SafeAreaView>
);

export default HomeScreen;